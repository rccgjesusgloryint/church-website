import React, { FormEvent, FormEventHandler } from "react";
import UploadFormTest from "./UploadFormTest";

type Props = {};

const Testing = (props: Props) => {
  return <UploadFormTest />;
};

export default Testing;
